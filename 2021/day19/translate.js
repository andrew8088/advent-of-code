const n = (x) => -1 * x;

const translations = [
  // 1, 2, 3
  ([x, y, z]) => [x, y, z],
  // 1, -2, -3
  ([x, y, z]) => [x, n(y), n(z)],
  // 1, 3, -2
  ([x, y, z]) => [x, z, n(y)],
  // 1, -3, 2
  ([x, y, z]) => [x, n(z), y],
  // -1, 2, -3
  ([x, y, z]) => [n(x), y, n(z)],
  // -1, -2, 3
  ([x, y, z]) => [n(x), n(y), z],
  // -1, 3, 2
  ([x, y, z]) => [n(x), z, y],
  // -1, -3, -2
  ([x, y, z]) => [n(x), n(z), n(y)],
  // 2, -1, 3
  ([x, y, z]) => [y, n(x), z],
  // 2, 1, -3
  ([x, y, z]) => [y, x, n(z)],
  // 2, 3, 1,
  ([x, y, z]) => [y, z, x],
  // 2, -3, -1
  ([x, y, z]) => [y, n(z), n(x)],
  // -2, 1, 3
  ([x, y, z]) => [n(y), x, z],
  // -2, -1, -3
  ([x, y, z]) => [n(y), n(x), n(z)],
  // -2, -3, 1
  ([x, y, z]) => [n(y), n(z), x],
  // -2, 3, -1
  ([x, y, z]) => [n(y), z, n(x)],
  // 3, -1, -2
  ([x, y, z]) => [z, n(x), n(y)],
  // 3, 1, 2
  ([x, y, z]) => [z, x, y],
  // 3, -2, 1
  ([x, y, z]) => [z, n(y), x],
  // 3, 2, -1
  ([x, y, z]) => [z, y, n(x)],
  // -3, 1, 2
  ([x, y, z]) => [n(z), x, y],
  // -3, -1, 2
  ([x, y, z]) => [n(z), n(x), y],
  // -3, -2, -1
  ([x, y, z]) => [n(z), n(y), n(x)],
  // -3, 2, 1
  ([x, y, z]) => [n(z), y, x],
];
